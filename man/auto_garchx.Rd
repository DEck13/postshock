% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auto-garchx.R
\name{auto_garchx}
\alias{auto_garchx}
\alias{auto_garch}
\title{Auto-select a GARCHX specification by BIC (with optional backcast refit)}
\usage{
auto_garchx(
  y,
  xreg = NULL,
  max.p = 3,
  max.q = 3,
  search_o = c(0L, 1L),
  final_refit = TRUE,
  clamp_factor = c(0.1, 10),
  vcov.type = c("ordinary", "robust"),
  verbose = TRUE
)

auto_garch(
  y,
  xreg = NULL,
  max.p = 3,
  max.q = 3,
  search_o = c(0L, 1L),
  final_refit = TRUE,
  clamp_factor = c(0.1, 10),
  vcov.type = c("ordinary", "robust"),
  verbose = TRUE
)
}
\arguments{
\item{y}{Numeric vector of returns/residuals (no NA/Inf/NaN).}

\item{xreg}{Optional matrix of exogenous regressors (same number of rows as \code{y}).}

\item{max.p, max.q}{Nonnegative integers; AR (\code{p}) and MA (\code{q}) orders to search up to.}

\item{search_o}{Integer vector with values in \verb{\{0,1\}}; 0 = symmetric, 1 = GJR asymmetry.}

\item{final_refit}{Logical; if \code{TRUE}, refit the winner using a backcast equal
to the unconditional variance (with clamping).}

\item{clamp_factor}{Length-2 numeric vector \code{c(low, high)}. If the computed
unconditional variance falls outside \verb{[low, high] * var(y)}, fall back to
the sample variance for the backcast.}

\item{vcov.type}{Character; \code{"ordinary"} or \code{"robust"} standard errors.}

\item{verbose}{Logical; print progress/info/warnings.}
}
\value{
A list with elements:
\itemize{
\item \code{fit} : the selected \code{garchx} fit object
\item \code{p}, \code{q}, \code{o} : selected orders
\item \code{bic}, \code{aic} : information criteria of the selected fit
\item \code{refit_used} : \code{TRUE/FALSE} if the final backcast refit replaced the grid one
}
}
\description{
Grid-search over \eqn{(q, p, o)} orders for \pkg{garchx} and pick the model
with the lowest BIC; optionally refit the selected model using a backcast
initialized at the unconditional variance (clamped to a reasonable range).
}
\details{
The order used by \pkg{garchx} is \code{c(q, p, o)} (MA, AR, asymmetry). We skip
the trivial \verb{(0,0,0)} model. If no model converges in the grid, the function
stops with an error.
}
\examples{
\donttest{
  set.seed(1)
  y <- scale(rnorm(500))[,1]
  out <- auto_garchx(y, max.p = 2, max.q = 2, search_o = c(0,1),
                     final_refit = TRUE, vcov.type = "robust", verbose = FALSE)
  c(out$p, out$q, out$o); out$bic
}

}
